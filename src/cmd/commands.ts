// @ts-nocheck
/* eslint-disable */

export type EventsShape<T extends Record<string, any>> = {
  [K in keyof T]: __EventObj__<T[K]> & {
    (handle: __WebviewWindow__): __EventObj__<T[K]>;
  };
};

export function makeLievt<T extends Record<string, any>>(ev: EventsShape<T>) {
  return function lievt<K extends keyof T>(key: K) {
    return (handler: (payload: T[K]) => void) => {
      const obj = ev[key] as __EventObj__<T[K]>;
      return obj.listen((e) => handler(e.payload));
    };
  };
}

// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE, Channel } from "@tauri-apps/api/core";
import * as __TAURI_EVENT from "@tauri-apps/api/event";

/** Commands */
export const commands = {
	audioPlay: (req: AudioPlayRequest) => typedError<AudioPlayAck, string>(__TAURI_INVOKE("audio_play", { req })),
	audioPause: () => typedError<null, string>(__TAURI_INVOKE("audio_pause")),
	audioResume: () => typedError<null, string>(__TAURI_INVOKE("audio_resume")),
	audioStop: () => typedError<null, string>(__TAURI_INVOKE("audio_stop")),
	audioStatus: () => typedError<AudioStatus, string>(__TAURI_INVOKE("audio_status")),
	audioDebugSpectrogram: (req: AudioDebugSpectrogramRequest) => typedError<AudioDebugSpectrogram, string>(__TAURI_INVOKE("audio_debug_spectrogram", { req })),
	audioDebugPipelineProbe: (req: AudioDebugProbeRequest) => typedError<AudioDebugProbeResult, string>(__TAURI_INVOKE("audio_debug_pipeline_probe", { req })),
	exists: (path: string) => typedError<boolean, string>(__TAURI_INVOKE("exists", { path })),
	allAudioRecursive: (folder: string) => typedError<string[], string>(__TAURI_INVOKE("all_audio_recursive", { folder })),
	resolveSavePath: () => typedError<string, string>(__TAURI_INVOKE("resolve_save_path")),
	updateSavePath: (newPath: string) => typedError<null, string>(__TAURI_INVOKE("update_save_path", { newPath })),
	appReady: () => __TAURI_INVOKE<void>("app_ready"),
	getWindowKind: () => __TAURI_INVOKE<WindowKindInfo>("get_window_kind"),
	createWindow: (name: WindowName, options: {
	width: number | null,
	height: number | null,
} | null) => __TAURI_INVOKE<void>("create_window", { name, options }),
	getMouseAndWindowPosition: () => typedError<MouseWindowInfo, string>(__TAURI_INVOKE("get_mouse_and_window_position")),
	ytdlpDownloadAndInstall: () => typedError<InstallResult, string>(__TAURI_INVOKE("ytdlp_download_and_install")),
	ytdlpCheckUpdate: () => typedError<CheckResult, string>(__TAURI_INVOKE("ytdlp_check_update")),
	checkExists: () => typedError<{
	installed_path: string,
	installed_version: string,
} | null, string>(__TAURI_INVOKE("check_exists")),
	githubOk: () => __TAURI_INVOKE<boolean>("github_ok"),
	lookMedia: (url: string) => typedError<MediaInfo, string>(__TAURI_INVOKE("look_media", { url })),
	testDownloadAudio: () => typedError<string[], string>(__TAURI_INVOKE("test_download_audio")),
	ffmpegCheckUpdate: () => typedError<FfCheck, string>(__TAURI_INVOKE("ffmpeg_check_update")),
	ffmpegDownloadAndInstall: () => typedError<InstallResult, string>(__TAURI_INVOKE("ffmpeg_download_and_install")),
	ffmpegVersion: () => typedError<string, string>(__TAURI_INVOKE("ffmpeg_version")),
	ffmpegCheckExists: () => typedError<{
	installed_path: string,
	installed_version: string,
} | null, string>(__TAURI_INVOKE("ffmpeg_check_exists")),
	create: (data: CollectMission) => typedError<null, string>(__TAURI_INVOKE("create", { data })),
	read: (name: string) => typedError<Playlist, string>(__TAURI_INVOKE("read", { name })),
	readAll: () => typedError<Playlist[], string>(__TAURI_INVOKE("read_all")),
	update: (data: CollectMission, anchor: Playlist) => typedError<null, string>(__TAURI_INVOKE("update", { data, anchor })),
	delete: (name: string) => typedError<null, string>(__TAURI_INVOKE("delete", { name })),
	fatigue: (music: Music) => typedError<null, string>(__TAURI_INVOKE("fatigue", { music })),
	boost: (music: Music) => typedError<null, string>(__TAURI_INVOKE("boost", { music })),
	cancleBoost: (music: Music) => typedError<null, string>(__TAURI_INVOKE("cancle_boost", { music })),
	cancleFatigue: (music: Music) => typedError<null, string>(__TAURI_INVOKE("cancle_fatigue", { music })),
	unstar: (list: Playlist, music: Music) => typedError<null, string>(__TAURI_INVOKE("unstar", { list, music })),
	resetLogits: () => typedError<null, string>(__TAURI_INVOKE("reset_logits")),
	deleteMusic: (music: Music) => typedError<null, string>(__TAURI_INVOKE("delete_music", { music })),
	recheckFolder: (entry: Entry) => typedError<Entry, string>(__TAURI_INVOKE("recheck_folder", { entry })),
	rmexclude: (list: Playlist, music: Music) => typedError<null, string>(__TAURI_INVOKE("rmexclude", { list, music })),
	updateWeblist: (entry: Entry, playlist: string) => typedError<Entry, string>(__TAURI_INVOKE("update_weblist", { entry, playlist })),
};

/** Events */
export const events = {
	//@type {ReturnType<typeof makeEvent<AudioEnded>>}
	audioEnded: makeEvent<AudioEnded>("audio-ended"),
	//@type {ReturnType<typeof makeEvent<AudioState>>}
	audioState: makeEvent<AudioState>("audio-state"),
	//@type {ReturnType<typeof makeEvent<FullScreenEvent>>}
	fullScreenEvent: makeEvent<FullScreenEvent>("full-screen-event"),
	//@type {ReturnType<typeof makeEvent<ProcessMsg>>}
	processMsg: makeEvent<ProcessMsg>("process-msg"),
	//@type {ReturnType<typeof makeEvent<ProcessResult>>}
	processResult: makeEvent<ProcessResult>("process-result"),
	//@type {ReturnType<typeof makeEvent<YtdlpVersionChanged>>}
	ytdlpVersionChanged: makeEvent<YtdlpVersionChanged>("ytdlp-version-changed"),
};

/* Types */export type AudioDebugProbeRequest = {
	path: string,
	target_lufs: number | null,
	track_lufs: number | null,
	track_true_peak_dbtp: number | null,
	offset_ms: number | null,
	capture_ms: number | null,
	sample_rate: number | null,
	channels: number | null,
};

export type AudioDebugProbeResult = {
	output_dir: string,
	metadata_json: string,
	raw_wav: string,
	processed_wav: string,
	output_wav: string,
	gain_db: number,
	playback_filter: string,
	sample_rate: number,
	channels: number,
	offset_ms: number,
	requested_capture_ms: number,
	actual_capture_ms: number,
	raw_stats: AudioDebugProbeStats,
	processed_stats: AudioDebugProbeStats,
	output_stats: AudioDebugProbeStats,
};

export type AudioDebugProbeStats = {
	sample_count: number,
	finite_sample_count: number,
	non_finite_count: number,
	over_unity_count: number,
	over_unity_ratio: number,
	peak_dbfs: number,
	rms_dbfs: number,
};

export type AudioDebugSpectrogram = {
	raw_data_url: string,
	processed_data_url: string,
	gain_db: number,
	playback_filter: string,
	width: number,
	height: number,
	duration_ms: number | null,
};

export type AudioDebugSpectrogramRequest = {
	path: string,
	target_lufs: number | null,
	track_lufs: number | null,
	track_true_peak_dbtp: number | null,
	width: number | null,
	height: number | null,
};

export type AudioEnded = {
	path: string,
};

export type AudioPlayAck = {
	path: string,
	duration_ms: number | null,
	gain: number,
};

export type AudioPlayRequest = {
	path: string,
	target_lufs: number | null,
	track_lufs: number | null,
	track_true_peak_dbtp: number | null,
};

export type AudioState = {
	path: string | null,
	playing: boolean,
	paused: boolean,
	position_ms: number,
	duration_ms: number | null,
};

export type AudioStatus = {
	path: string | null,
	playing: boolean,
	paused: boolean,
	position_ms: number,
	duration_ms: number | null,
};

export type CheckResult = {
	installed_path: string | null,
	installed_version: string | null,
	latest_version: string | null,
	needs_update: boolean,
	asset_name: string,
	download_url: string,
};

export type CollectMission = {
	name: string,
	folders: FolderSample[],
	links: LinkSample[],
	entries: Entry[],
	exclude: Music[],
};

export type CreateWindowOptions = {
	width: number | null,
	height: number | null,
};

export type Entry = {
	path: string | null,
	name: string,
	musics: Music[],
	avg_db: number | null,
	url: string | null,
	downloaded_ok: boolean | null,
	tracking: boolean | null,
	entry_type: EntryType,
};

export type EntryType = "Local" | "WebList" | "WebVideo" | "Unknown";

export type FfCheck = {
	installed_path: string | null,
	latest_tag: string | null,
	needs_install: boolean,
	asset_name: string | null,
	download_url: string | null,
	note: string | null,
};

export type FolderSample = {
	path: string,
	items: string[],
};

export type FullScreenEvent = {
	is_fullscreen: boolean,
};

export type InstallResult = {
	installed_path: string,
	installed_version: string,
};

export type LinkSample = {
	url: string,
	title_or_msg: string,
	entry_type: EntryType,
	count: number | null,
	status: LinkStatus | null,
	tracking: boolean,
};

export type LinkStatus = "Ok" | "Err";

export type MediaInfo = {
	title: string,
	item_type: string,
	entries_count: number | null,
};

export type MouseWindowInfo = {
	mouse_x: number,
	mouse_y: number,
	window_x: number,
	window_y: number,
	window_width: number,
	window_height: number,
	rel_x: number,
	rel_y: number,
	pixel_ratio: number,
};

export type Music = {
	path: string,
	title: string,
	avg_db: number | null,
	true_peak_dbtp: number | null,
	base_bias: number,
	user_boost: number,
	fatigue: number,
	diversity: number,
};

export type Playlist = {
	name: string,
	avg_db: number | null,
	entries: Entry[],
	exclude: Music[],
};

export type ProcessMsg = {
	playlist: string,
	str: string,
};

export type ProcessResult = {
	working_path: string,
	saved_path: string,
	name: string,
	playlist: string,
};

export type WindowKindInfo = {
	window: WindowName | null,
	is_prewarm: boolean,
	label: string,
	is_primary_main: boolean,
};

export type WindowName = "Main";

export type YtdlpVersionChanged = {
	str: string,
};

/* Tauri Specta runtime */
async function typedError<T, E>(result: Promise<T>): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
    try {
        return { status: "ok", data: await result };
    } catch (e) {
        if (e instanceof Error) throw e;
        return { status: "error", error: e as any };
    }
}

function makeEvent<T>(name: string) {
    const base = {
        listen: (cb: __TAURI_EVENT.EventCallback<T>) => __TAURI_EVENT.listen(name, cb),
        once: (cb: __TAURI_EVENT.EventCallback<T>) => __TAURI_EVENT.once(name, cb),
        emit: ((payload: T) => __TAURI_EVENT.emit(name, payload) as unknown) as (T extends null ? () => Promise<void> : (payload: T) => Promise<void>)
    };

    const fn = (target: import("@tauri-apps/api/webview").Webview | import("@tauri-apps/api/window").Window) => ({
        listen: (cb: __TAURI_EVENT.EventCallback<T>) => target.listen(name, cb),
        once: (cb: __TAURI_EVENT.EventCallback<T>) => target.once(name, cb),
        emit: ((payload: T) => target.emit(name, payload) as unknown) as (T extends null ? () => Promise<void> : (payload: T) => Promise<void>)
    });

    return Object.assign(fn, base);
}

